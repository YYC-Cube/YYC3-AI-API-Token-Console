# YYC³ Token Console — 环境变量清单 (Phase 3 / Task 3.5 doctor 配套)
# ====================================================================
# 本项目为纯前端 SPA, 构建期注入 (Vite import.meta.env), 运行时只读。
# 复制为 .env.local 后按需填写; 所有 VITE_ 前缀变量会进产物 — 禁止放真实密钥。

# ── 构建基路径 (pages.yml 构建时注入, 本地一般无需设置) ──
# VITE_BASE=/

# ── 测试开关 (integration 分级测试显式开启, CI 默认不跑) ──
# YYC3_TEST_INTEGRATION=1

# ── 后端端点 (可选 — 默认走内置 Mock/本地闭环, 见 src/app/lib/api-config.ts) ──
# 用户可在应用「系统设置」页运行时编辑, 以下仅作首次默认值提示
# WS_ENDPOINT=ws://localhost:3113/ws
# AI_BASE=https://api.openai.com/v1

# ── 部署服务器 CORS 收敛 (deploy/server.mjs 运行时读取, 与构建产物无关) ──
# 默认仅同源 (不回 CORS 头); 需要跨域访问 Ollama 代理时按需放行:
# ALLOW_ORIGIN=https://token.yyc3.vip,http://192.168.3.5:5173   # 精确来源白名单 (逗号分隔)
# ALLOW_ORIGIN_CIDR=192.168.3.0/24,10.0.0.0/8                   # IPv4 网段 (内网场景)
# ALLOW_ORIGIN="*"                                              # 放行任意来源 (仅限可信内网!)
